<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recipes</title>
</head>
<body>
    <header>
        <a href='/auth/login'>Login</a>
        <h1>Recipe App</h1>
        <a href='/auth/signup'>Sign Up</a>
    </header>
    <main>
        <h2><%=recipe.title%></h2>
        <h3>Ingredients</h3>
        <ul>
            <% recipe.Ingredients.forEach(ingredient => { %> 
                <li><%=ingredient.name%> &nbsp;&nbsp;&nbsp; <%=ingredient.RecipeIngredient.quantity%> <%=ingredient.RecipeIngredient.units%></li>
            <% }) %>
        </ul>
    </main>
</body>
</html>